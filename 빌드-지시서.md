# 빌드 지시서: 기후·재난 비즈니스 아이디어 포트폴리오 웹페이지

이 문서는 Claude Code에게 주는 작업 지시서다. 함께 제공되는 `ideas-data.js`가 콘텐츠 원본이다.

## 0. 절대 규칙 (가장 중요)

1. **콘텐츠를 새로 쓰거나 윤색하지 않는다.** 모든 텍스트는 `ideas-data.js`에서 그대로 가져온다. 문장 추가·요약·수치 생성 금지.
2. **참고문헌 URL을 절대 만들어내지 않는다.** `references`에 `url`이 있는 항목만 링크로 렌더링한다. `url: null`(미검증) 항목은 링크 없이 텍스트 + "※출처 미확인 — 검증 필요" 배지로 표시한다. 이 페이지의 신뢰성은 "링크가 전부 실재하고 내용과 일치한다"에 걸려 있다.
3. `verified: false`인 수치가 포함된 아이디어의 상세 페이지 상단에는 안내 박스를 넣는다: "이 아이디어의 일부 수치는 아직 출처 검증 전입니다(※ 표시)."

## 1. 산출물 형태

- 정적 사이트. GitHub Pages 배포 대상.
- 파일 구조 (NHD 프로젝트와 같은 패턴):
  - `index.html` — 헤더 + 필터 + 카드 그리드
  - `ideas/<id>.html` — 아이디어별 상세 페이지 (빌드 스크립트로 데이터에서 생성해도 되고, `detail.html?id=` 방식 대신 반드시 아이디어별 개별 파일로 — 개별 URL 공유 목적)
  - `ideas-data.js` — 제공된 파일 그대로 사용 (수정 금지)
  - 공통 CSS는 `style.css` 하나로 분리
- 빌드 도구 없이 브라우저에서 바로 열려야 한다 (프레임워크 금지, 순수 HTML/CSS/JS).

## 2. index.html

- **헤더**: 제목만. "기후·재난 비즈니스 아이디어 포트폴리오" (h1). 부가 문구·전제 설명 없음.
- **필터**: 탭/버튼 5개 — 전체 · 폭염 · 정전 · 홍수 · 가뭄. 단일 선택. 그 외 필터 없음.
- **카드 그리드**: 반응형 (auto-fill, minmax(320px,1fr)).
- **카드 내용**: ① 카테고리 색 표시(카드 상단 색 바 또는 좌측 보더) ② 아이디어 이름(title) ③ 한 줄 요약(subtitle). **이것만.** 난이도·학생유형·대회 배지 등 다른 정보 표시 금지.
- 카드에 배정 학생이 저장돼 있으면 우측 상단에 작은 배지로 `👤 이름` 표시 (아래 §4).
- 카드 클릭 → `ideas/<id>.html`로 이동.

## 3. 상세 페이지 (ideas/<id>.html)

섹션 순서 (데이터 필드 → 화면 제목):
1. 상단: 카테고리 배지 + title + subtitle + `region` (있을 때)
2. `concept` → "개념"
3. `science` → "과학 원리"
4. `numbers` → "핵심 숫자" — 본문 속 `[숫자]`는 하단 참고문헌 번호와 대응. `(추정)` 표시된 계산은 배지 스타일로 강조.
5. `economics` → "해야 하는 이유"
6. `checks` → "※ 확인 필요" — 리스트
7. `references` → "참고문헌" — 번호 목록. url 있으면 새 탭 링크(`target="_blank" rel="noopener"`), 없으면 텍스트 + 미확인 배지.
8. `related` → "연관 아이디어" — 해당 상세 페이지로 링크 (있을 때만)
9. 교사 메모 영역 (§4)
10. "← 목록으로" (index.html 링크, 상단·하단 모두)

## 4. 교사 메모·학생 배정 기능

- 상세 페이지마다: **배정 학생** 텍스트 입력 1개 + **메모** textarea 1개. 입력 즉시(디바운스 500ms) localStorage에 저장.
- 저장 키: `portfolio-notes-v1` 하나에 `{ "<id>": {student: "...", memo: "...", updated: "ISO날짜"} }` 형태로 통합 저장.
- localStorage는 브라우저별 저장이므로, **index.html 헤더 우측에 두 버튼**을 둔다:
  - "메모 내보내기" → 위 객체를 JSON 파일로 다운로드 (파일명: `portfolio-notes-YYYYMMDD.json`)
  - "메모 가져오기" → JSON 파일 업로드 → localStorage에 병합(같은 id는 가져온 쪽으로 덮어쓰기, 덮어쓰기 전 confirm)
- 메모/학생이 있는 카드는 index에서 배지 표시 (§2).
- 이유를 index 하단에 작게 한 줄 안내: "메모는 이 브라우저에만 저장됩니다. 다른 기기와 공유하려면 내보내기/가져오기를 사용하세요."

## 5. 스타일 가이드 (권장 — NHD의 녹색/금색과 다른 톤)

- 배경 `#FBF8F3` (따뜻한 종이색), 본문 잉크 `#26221C`, 보조 텍스트 `#6E675C`
- 카드: 흰색, `1px solid #E9E2D6`, radius 12px, hover 시 살짝 떠오름(그림자 + translateY(-2px))
- 카테고리 색 (색 바·배지·필터 활성 상태 공통):
  - 폭염 `#E4572E` / 연한 배경 `#FDEAE3`
  - 정전 `#4B4E9E` / `#EAEAF6`
  - 홍수 `#2278B5` / `#E4F0F9`
  - 가뭄 `#C2872B` / `#F8EFDE`
- 헤더: 배경색 없이 배경지 위에 큰 제목만 (font-weight 800). 아래 얇은 구분선.
- 폰트: Pretendard CDN (`https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.min.css`), fallback 시스템 폰트.
- 미확인 배지: `#FFF4E5` 배경 / `#8B5A00` 텍스트, "※미확인".
- (추정) 배지: `#EEF2FF` 배경 / `#3730A3` 텍스트.
- 모바일(≤640px): 카드 1열, 상세 페이지 좌우 패딩 축소.
- 인쇄 CSS: 메모 입력창·버튼 숨김, 링크 URL 병기.

## 6. 접근성·기타

- 모든 인터랙션은 JS 없이도 링크로 동작하는 것 우선(카드 = `<a>`).
- `esc()` 이스케이프 후 렌더링 (NHD 코드와 동일하게 XSS 방지).
- 파비콘·og 태그는 제목 텍스트 기반으로 간단히.

## 7. 배포 (GitHub Pages)

1. 새 저장소 생성 → 파일 push
2. Settings → Pages → Branch: main, 폴더: / (root)
3. 생성된 `https://<계정>.github.io/<저장소>/` 링크 확인
4. 상세 페이지 상대경로(`ideas/...` ↔ `../index.html`)가 Pages 하위 경로에서 깨지지 않는지 확인

## 8. 완료 확인 체크리스트

- [ ] 카드 30개 전부 렌더링, 필터 4종 정상 작동
- [ ] 상세 페이지 30개 생성, 뒤로가기 정상
- [ ] url 없는 참고문헌이 링크로 렌더링되지 않음 (전수 확인)
- [ ] 메모 저장 → 새로고침 후 유지 → 내보내기 JSON → 가져오기 병합 정상
- [ ] 모바일 폭에서 레이아웃 확인
